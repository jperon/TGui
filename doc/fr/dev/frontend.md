# Interface SPA — référence détaillée

> Fichier généré automatiquement. Ne pas modifier manuellement.
>
> Généré le: 2026-03-12 10:33:27 UTC

Organisation des modules CoffeeScript et flux UI principaux.

| Module | Résumé |
|---|---|
| `frontend/src/app.coffee` | (pas de description en tête de fichier) |
| `frontend/src/app_data_helpers.coffee` | (pas de description en tête de fichier) |
| `frontend/src/app_fields_helpers.coffee` | (pas de description en tête de fichier) |
| `frontend/src/app_sidebar_helpers.coffee` | (pas de description en tête de fichier) |
| `frontend/src/app_snapshot_helpers.coffee` | (pas de description en tête de fichier) |
| `frontend/src/app_undo_helpers.coffee` | (pas de description en tête de fichier) |
| `frontend/src/app_view_helpers.coffee` | (pas de description en tête de fichier) |
| `frontend/src/auth.coffee` | (pas de description en tête de fichier) |
| `frontend/src/graphql_client.coffee` | (pas de description en tête de fichier) |
| `frontend/src/i18n.coffee` | (pas de description en tête de fichier) |
| `frontend/src/modal.coffee` | (pas de description en tête de fichier) |
| `frontend/src/spaces.coffee` | (pas de description en tête de fichier) |
| `frontend/src/views/custom_view.coffee` | (pas de description en tête de fichier) |
| `frontend/src/views/data_view.coffee` | (pas de description en tête de fichier) |
| `frontend/src/yaml_builder.coffee` | (pas de description en tête de fichier) |
